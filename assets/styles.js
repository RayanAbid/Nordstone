import {StyleSheet, StatusBar} from 'react-native';

const color = {
  primary: 'black',
  secondary: '#000000',
  light: '#9098B1',
  textWhite: '#FFFFFF',
  backgroundCol: '#fff',
};

export default StyleSheet.create({
  textColSpecial: {
    color: color.primary,
  },
  imageRegis: {
    width: 150,
    height: 150,
    borderRadius: 10,
  },
});
